services:
  remoteclaw:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - remoteclaw-data:/app/data
    environment:
      - DB_PATH=/app/data/remoteclaw.db
      - PORT=3000
      - HOST_URL=http://localhost:3000
    restart: unless-stopped

volumes:
  remoteclaw-data:
